context Customers is {
  entity Customer is {
    record mainType is { id is CustomerId }
    state main of record mainType
    handler input is { ??? }
  } with { explained as {
  | An entity for customers that tracks the state changes of the entity.
  }}
} with { explained as {
| Customer context represents the customer of the Reactive BBQ.
}}
