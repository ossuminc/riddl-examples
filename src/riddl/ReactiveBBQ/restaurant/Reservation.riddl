context Reservation is {
  record ReservationValue is {
    partyName is String,
    phoneNumber is String,
    location is Id(Location),
    time is Time,
    date is Date
  }
  entity Reservation is {
   options ( aggregate )
   state reservation of record ^^.ReservationValue is {
     handler ofInputs is {}
   }
  }
}
// #Reservation
